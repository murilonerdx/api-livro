dist: trusty
language: java
sudo: true
jdk: openjdk11
install:
  - ./mvnw install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
before_install:
  - chmod +x mvnw
after_success:
  - bash <(curl -s https://codecov.io/bash)